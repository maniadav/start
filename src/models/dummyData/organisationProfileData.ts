import { IOrganisationProfile } from "../organisation.profile.model";

// Empty array - admin will create organizations through the system
export const dummyOrganisationProfiles: Partial<IOrganisationProfile>[] = [];
